using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Diode : MonoBehaviour {
    // Main functional already works standalone,
    // but there you could make more stuff if it is necessary OwO
    private void Start() {
        Debug.Log("Thrown Diode");
    }
}

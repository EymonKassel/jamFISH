using UnityEngine;

public class QuestItem : MonoBehaviour {
    // Main functional already works in "RepairArea",
    // but there you could make more stuff if it is necessary OwO
    private void Start() {
        Debug.Log("Thrown QuestItem");
    }
}

using UnityEngine;

public class HugeStone : MonoBehaviour {
    // Main functional already works in "DestructibleStructure",
    // but there you could make more stuff if it is necessary OwO
    private void Start() {
        Debug.Log("Thrown HugeStone");
    }
}

using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bait : MonoBehaviour {
    // Main functional already works in "EnemyPatrol",
    // but there you could make more stuff if it is necessary OwO
    private void Start() {
        Debug.Log("Thrown Bait");
    }
}
